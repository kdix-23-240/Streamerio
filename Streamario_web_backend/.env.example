# Server
PORT=8888
FRONTEND_URL=*

# Database (Supabase/Postgres)
# 推奨: Supabase ダッシュボードの接続文字列をそのまま設定
# 例: postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:5432/postgres?sslmode=require
DATABASE_URL=

# または別名（DATABASE_URLが未設定の場合のみ使用）
# SUPABASE_DB_URL=

# 個別指定で組み立てる場合（DATABASE_URL未設定時のみ使用）
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=streamerio
# Supabase 利用時は require を推奨（ローカルDBは disable 可）
DB_SSLMODE=require

# Redis
REDIS_URL=localhost:6379
